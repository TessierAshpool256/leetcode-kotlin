## 11. Container With Most Water
**[⇐](../README.md)** |
<span style="color:orange">Medium</span> |

---
### En
You are given an integer array `height` of length `n`. There are `n` vertical lines drawn such that the two endpoints of the `i'th` line are `(i, 0)` and `(i, height[i])`.

Find two lines that together with the `x-axis` form a container, such that the container contains the most water.

Return the maximum amount of water a container can store.

Notice that you may not slant the container.

---
### Ru
Вам дан массив целых чисел `height` длиной `n`. Нарисованы `n` вертикальных линий так, что два конца `i‑й` линии находятся в точках `(i, 0)` и `(i, height[i])`.

Найдите две линии, которые вместе с осью `x` образуют контейнер, содержащий максимальное количество воды.

Верните максимальный объём воды, который может удержать контейнер.

Замечание: наклонять контейнер нельзя.

---

[Solution_011.kt](../app/src/main/kotlin/org/tessier/ashpool/leetcode/solution/Solution_011.kt)
