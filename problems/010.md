## 10. Regular Expression Matching

<span style="color:red">Hard</span>

---
### En
Given an input string `s` and a pattern `p`, implement regular expression matching with support for `.` and `*` where:

- `.` Matches any single character.
- `*` Matches zero or more of the preceding element.

The matching should cover the entire input string (not partial).

---
### Ru
Дано входная строка `s` и шаблон `p`. Реализуйте сопоставление регулярных выражений с поддержкой `.` и `*`, где:

- `.` соответствует любому одиночному символу.
- `*` соответствует нулю или более повторений предшествующего элемента.

Сопоставление должно охватывать всю входную строку (не частично).

---

[Solution_010.kt](../app/src/main/kotlin/org/tessier/ashpool/leetcode/solution/Solution_010.kt)
